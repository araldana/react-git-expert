
//export const GitItem = (props) => {
//export const GitItem = (image) => {
export const GitItem = ({title, url}) => {
//    console.log(props);
//    console.log(image);
      console.log ({title, url});

  return (
    <div>GitItem</div>
  );
}
